# Breathing AI

A computer vision project that uses your webcam to detect and classify breathing phases in real time.

YOLOv8 tracks your body from the side, optical flow measures how much your shoulder, chest, and belly move between frames, and a neural network classifies those movement signals into four breathing states: **inhale**, **exhale**, **hold in**, and **hold out**.

---

## How It Works

```
Webcam -> YOLOv8 finds shoulder + hip -> 3 boxes placed on shoulder / chest / belly
       -> optical flow measures x-direction movement inside each box
       -> scipy smooths the 3 signals
       -> data normalized + sliced into 60-frame windows (~3 seconds)
       -> Conv1D + LSTM neural network trained on those windows
       -> live predictions from webcam
```

---

## Project Status

| Step | Description | Status |
|------|-------------|--------|
| 1 | Environment setup | Done |
| 2 | Data collection — 6 side-view sessions | Done |
| 3 | Data processing & smoothing | Done |
| 4 | Labeling | Done (real-time, during recording) |
| 5 | Train model | Done |
| 6 | Live integration | Done |
| 7 | Test & improve | Done |

---

## Folder Structure

```
breathing-ai/
|
+-- data/
|   +-- side/                          # Side-view CSV sessions
|   |   +-- session_001.csv
|   |   +-- session_002.csv
|   |   +-- ...
|   +-- processed/
|       +-- dataset.npz                # Processed training data (generated by process_data.py)
|
+-- models/
|   +-- yolov8n-pose.pt               # YOLOv8 pose model (auto-downloaded by ultralytics)
|   +-- breathing_model.pt            # Trained breathing classifier
|
+-- scripts/
|   +-- collect_data.py               # Step 2 -- webcam recording with real-time labeling
|   +-- process_data.py               # Step 3 -- smooth, normalize, slice into windows
|   +-- train_model.py                # Step 5 -- train the Conv1D + LSTM model
|   +-- predict_live.py               # Step 6 -- live webcam breathing prediction
|   +-- analyze_session.py            # Step 7 -- plot + summarize a session log CSV
|   +-- test_yolo.py                  # Diagnostic -- verify YOLOv8 detects keypoints correctly
|
+-- logs/
|   +-- session_YYYYMMDD_HHMMSS.csv  # Frame-by-frame predictions (auto-saved on quit)
|   +-- summary_YYYYMMDD_HHMMSS.csv  # Phase transitions with durations (auto-saved on quit)
|
+-- readme.md
```

---

## Why Side View + Optical Flow?

Earlier versions tracked face and shoulder landmarks from the front using MediaPipe. The signal was weak — the chest barely moves in the Y direction when sitting at a desk.

Switching to a **side view** with **optical flow boxes** gave a much stronger signal:

| Approach | inhale-exhale gap |
|---|---|
| Front view landmarks (MediaPipe) | ~0.001 |
| Side view optical flow (YOLOv8) | ~0.105 |

Optical flow measures how pixels actually move between frames — no landmarks needed for the signal itself. YOLOv8 is only used to position the 3 boxes on the right body area automatically.

---

## CSV Data Format

Each session file contains one row per camera frame:

| Column | Description |
|--------|-------------|
| `frame` | Frame number |
| `shoulder_flow` | Mean x-direction optical flow in the shoulder box |
| `chest_flow` | Mean x-direction optical flow in the chest box |
| `belly_flow` | Mean x-direction optical flow in the belly box |
| `timestamp` | Seconds elapsed since recording started |
| `label` | `inhale` / `exhale` / `hold_in` / `hold_out` |

Positive flow = body expanding forward (inhale direction).
Negative flow = body contracting (exhale direction).
Near zero = no movement (hold phases).

---

## Processed Dataset Format

`data/processed/dataset.npz` is a compressed numpy file:

| Array | Shape | Description |
|---|---|---|
| `X` | `(num_windows, 60, 12)` | Training windows, each 60 frames x 12 features |
| `y` | `(num_windows,)` | Label: 0=inhale, 1=exhale, 2=hold_in, 3=hold_out |

Generated by running `process_data.py`. Re-run it any time you add new sessions.

---

## Model Architecture

- **Input:** 60-frame sliding window, **12 features** per frame:
  - 3 signed-normalized flow values (range [-1, 1], preserves direction)
  - 3 std-dev values — how noisy/active the signal is
  - 3 mean-absolute values — overall movement magnitude (near zero = hold)
  - 3 signed-mean values — net direction across the window (positive = inhale, negative = exhale)
- **Conv1D(12 -> 32, kernel=5)** — detects local patterns (rising/falling signal)
- **MaxPool1D** — downsamples from 60 to 30 time steps
- **LSTM(32 -> 64)** — learns patterns across the sequence over time
- **Dropout(0.3)** — prevents overfitting
- **Dense(64 -> 4)** — outputs one score per class
- **Softmax** — converts scores to probabilities

**Hold detection override:** if the mean absolute chest flow is below 0.020, the prediction is forced to a hold phase regardless of model output. Hold type (hold_in vs hold_out) is determined by the last active label before the hold started — not by the model, since both holds look identical in the signal (near-zero movement).

---

## Libraries

| Library | Purpose |
|---------|---------|
| `opencv-python` | Reads webcam, computes optical flow, draws boxes and text |
| `ultralytics` | YOLOv8 — finds shoulder and hip keypoints to position boxes |
| `numpy` | Fast math on large arrays — backbone of all data processing |
| `pandas` | Load and inspect CSV files |
| `scipy` | Signal smoothing — Savitzky-Golay filter removes noise from flow signals |
| `torch` | PyTorch — builds and trains the neural network |
| `matplotlib` | Plot charts to visualize signals |
| `jupyter` | Interactive notebook environment |

---

## Requirements

- **Python 3.12** (3.13+ is too new — some libraries are not compatible yet)
- A webcam
- Windows / macOS / Linux

---

## Installation

**1. Clone or download this project**

**2. Install all dependencies using Python 3.12 specifically**

```bash
py -3.12 -m pip install opencv-python ultralytics scipy numpy pandas matplotlib torch jupyter
```

**3. Verify everything installed correctly**

```bash
py -3.12 -c "import cv2, ultralytics, torch; print('All good! PyTorch:', torch.__version__)"
```

**4. That's it.** The YOLOv8 pose model is downloaded automatically on first run.

> **Note:** Always use `py -3.12` to run scripts, not just `python` or `py`.

---

## Recording Data

```bash
py -3.12 scripts/collect_data.py
```

- Set `VIEW = 'side'` at the top of the script (default)
- Sit **sideways** to the camera — your shoulder and hip must both be visible
- Three colored boxes appear on your torso — orange (shoulder), cyan (chest), green (belly)
- Press a key when each breathing phase starts — the label stays until you press the next key
- **W** = inhale &nbsp; **S** = exhale &nbsp; **E** = hold_in &nbsp; **D** = hold_out
- Press **Q** to stop — data is saved automatically to `data/side/session_XXX.csv`

**Tips:**
- Camera at waist/chest height, slightly to your side
- Breathe deliberately so the chest and belly clearly expand
- Aim for 10-15 full breathing cycles per session (~2-3 minutes)
- Press the label key **when you see your body start moving** — not when you intend to breathe. The model sees movement, not intention. Pressing slightly late is better than pressing early.

---

## Processing Data

Run after recording new sessions to regenerate the training dataset:

```bash
py -3.12 scripts/process_data.py
```

Reads all CSVs from `data/side/`, smooths the flow signals, normalizes per-session, slices into 60-frame windows, saves `data/processed/dataset.npz`.

---

## Training the Model

```bash
py -3.12 scripts/train_model.py
```

Trains the Conv1D + LSTM model and saves the best weights to `models/breathing_model.pt`.
Key settings in the script: `EPOCHS`, `BATCH_SIZE`, `LR` (learning rate).

---

## Live Prediction

```bash
py -3.12 scripts/predict_live.py
```

- Opens your webcam and runs the trained model in real time
- Sit sideways — same position as during recording
- Top-right: current predicted label + confidence (e.g. `exhale 91%`)
- Left side: confidence bars for all 4 classes
- Waits ~3 seconds on startup to fill the 60-frame buffer before predicting
- If you move out of frame, the buffer resets automatically
- Box positions are locked on first detection — press **X** to reposition, **Q** to quit
- On quit, two files are saved automatically:
  - `logs/session_YYYYMMDD_HHMMSS.csv` — frame-by-frame predictions with probabilities
  - `logs/summary_YYYYMMDD_HHMMSS.csv` — each phase with start time, end time, and duration

---

## Analyzing a Session

```bash
py -3.12 scripts/analyze_session.py
```

Loads the most recent session log and shows:
- **Console summary** — seconds spent in each phase, transition count
- **Probability plot** — all 4 confidence lines over time
- **Label timeline** — color-coded bands showing the predicted phase sequence

Pass a specific file to analyze an older session:

```bash
py -3.12 scripts/analyze_session.py logs/session_20260311_191913.csv
```

For box breathing (4-4-4-4), you should see 4 roughly equal-width colored bands repeating: inhale → hold_in → exhale → hold_out.
